#include <lv/Graphics/Color.hpp>

namespace lv
{
	Color const Color::white(255);

	Color const Color::black(0);

	Color const Color::red(255, 0, 0);

	Color const Color::green(0, 255, 0);

	Color const Color::blue(0, 0, 255);
}